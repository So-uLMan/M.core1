from setuptools import setup, find_packages

setup(
    name="mcore",
    version="0.1.0",
    author="مصطفى",
    description="نواة الذكاء الحر – M.Core",
    packages=find_packages(),
    python_requires=">=3.8",
)
